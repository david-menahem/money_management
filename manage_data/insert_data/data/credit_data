    # credit_card = CreditCard(None, 37000, CreditCardEnum.MAX, 1000)
    # credit_cardDAO.insert(credit_card)
    # credit_card = CreditCard(None, 5000, CreditCardEnum.MAX, 500)
    # credit_cardDAO.insert(credit_card)
    # credit_card = CreditCard(None, 2000, CreditCardEnum.MAX, 500)
    # credit_cardDAO.insert(credit_card)
    # credit_card = CreditCard(None, 5000, CreditCardEnum.MAX, 200)
    # credit_cardDAO.insert(credit_card)
    # credit_card = CreditCard(None, 33000, CreditCardEnum.ISRACARD, 1050)
    # credit_cardDAO.insert(credit_card)
    # credit_card = CreditCard(None, 3000, CreditCardEnum.CAL, 600)
    # credit_cardDAO.insert(credit_card)